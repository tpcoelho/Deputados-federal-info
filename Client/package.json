{
  "name": "deputados-info",
  "version": "1.0.0",
  "description": "Angularjs frontend  -a localhost -p 8000 -c-1",
  "dependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server ./app",
    "postinstall": "bower install"
  }
}
